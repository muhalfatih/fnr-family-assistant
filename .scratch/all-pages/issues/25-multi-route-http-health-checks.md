# 25: Multi-Route HTTP Health Checks and Endpoint Contracts

**What to build:** Automated HTTP smoke test verifying 200 OK responses across all 5 top-level pages and validating GET/POST request handling for backend API endpoints.

**Blocked by:** 01 through 23

**Status:** ready-for-agent

- [ ] HTTP 200 OK responses on `/`, `/assets`, `/vault`, `/family`, and `/logs`.
- [ ] Verified GET and POST handling on `/api/documents/remind`.
- [ ] Verified GET and POST handling on `/api/bot/tasks`.
