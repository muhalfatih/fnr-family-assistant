# 14: Unified Compact Single-Row Vault Filter Toolbar

**What to build:** A single-row responsive filter toolbar on `/vault` combining document keyword search, category `<Select>` dropdown, and status filter pills, eliminating vertical scroll clutter and keeping cards above the fold.

**Blocked by:** 02: Design Tokens, Strict Spacing Scale & Tabular Numerals

**Status:** ready-for-agent

- [ ] Responsive single-row toolbar layout (`flex-col lg:flex-row`).
- [ ] Search input with instant keyword matching on title and document number.
- [ ] Category dropdown selector using Radix UI `<Select>`.
- [ ] Status filter pills with embedded count badges.
